# Using the preprocessor

## Installation
- `make pre`
<sub>The preprocessor depends on having the library folder made and the interepreter built.</sub>

## Usage
```bash
$ cobaltpre FILE OUTPUT -ARGS=VALUE -ARGS=VALUE...
```
<sub>Replace `FILE` with the file you want to preprocess and `OUTPUT` with the file you want to output to.</sub>
