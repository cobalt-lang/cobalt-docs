# One line statements
If a statement is just one line like
```js
if (true){
    print("True") // This is one line
}
```
the `{ }` are optional like in JS or C
```js
if (true)
    print("True") // This is one line
```
***
This works with `for`, `while`, and `if` statements.