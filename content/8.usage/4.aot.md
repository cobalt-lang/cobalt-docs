# AOT
The AOT compiler converts a Cobalt program to ASM source.

## Usage
```bash
cobaltaot file.cobalt -o file.S
```
the outputted file is going to be the bytecode converted to ASM that can then be
compiled further to .o, .so (.dll), and even executables. 