# Using lua-cobalt
## Usage
```bash
lua-cobalt lua > cobalt
```
Thats pretty much it, lua-cobalt cannot compile code inside of `loadstring` or `loadfile`, but it can compile code from a file. 

Not all distributions have lua-cobalt, it is optional.